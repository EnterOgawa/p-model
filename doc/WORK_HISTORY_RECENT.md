# WORK HISTORY RECENT
This file starts with an ASCII-only preamble to avoid a known Windows tool unicode slicing issue.
Japanese content begins after this preamble.

# 作業履歴（直近3ログ）

このファイルは AI が作業開始時に参照する **直近3ログ**（`doc/WORK_HISTORY.md` から自動抽出）である。
完全な履歴は `doc/WORK_HISTORY.md` を参照する。

---

[work] 2026-04-08T11:02:24+00:00 ユーザー指示『公開する対象のスクリプトに誤りが無いかを厳密に確認する』に対応し、今回の branch は computation ではなく **release-target script static sanity audit + publish-blocker classification** と判定した。理由は、新しい理論計算や検証更新ではなく、release 候補 script 群が構文・コメント・style・top-level 実行の観点で公開可能かを strict に確認する pure release-prep task だったからである。監査対象は前回と同じく [release_script_python_diff.txt](C:/develop/waveP/output/private/summary/release_script_python_diff.txt) の 229 本を正とし、量子については論文とリンクする [quantum_scripts_paper_linked.txt](C:/develop/waveP/output/private/summary/quantum_scripts_paper_linked.txt) の 120 本も個別に確認した。まず `py_compile` を inline Python で両 manifest に対して実行し、`release_diff_py_compile_checked=229 / failed=0`、`quantum_linked_py_compile_checked=120 / failed=0` を確認した。次に `rg -n "^(<<<<<<<|=======|>>>>>>>)|TODO|FIXME|NotImplementedError" --glob "scripts/**/*.py" --glob "sitecustomize.py"` を実行し、merge residue・TODO/FIXME・未実装例外の残存がないことを確認した。style 監査では `python -B scripts/summary/enforce_python_block_spacing.py --paths-file output/private/summary/release_script_python_diff.txt` を実行したところ [paper_html.py](C:/develop/waveP/scripts/summary/paper_html.py) に 2 箇所の block spacing 残差が見つかったため、`python -B scripts/summary/enforce_python_block_spacing.py --paths scripts/summary/paper_html.py --fix` を適用し、再監査で `files=229 changed=0 violations=0` を確認した。さらに top-level 実行の静的分類を AST で行い、release 差分全体で main guard 外の `Expr` は `enable_japanese_figure_localization()` 31 件、`matplotlib.use(...)` 11 件、`os.environ.setdefault(...)` 1 件、`sitecustomize.py` の環境初期化フック群のみであることを確認した。量子 subset 側でも main guard 外の `Expr` は `enable_japanese_figure_localization()` 27 件、`matplotlib.use(...)` 1 件、`os.environ.setdefault(...)` 1 件だけであり、いずれも import 時の意図しない本計算ではなく font/localization の初期化であると判定した。main guard 外の `if` も `if str(ROOT) not in sys.path:` 系の path bootstrap と `if __name__ == "__main__":` に限られていた。したがって、release 対象 script 群に publish blocker となる構文エラー・merge residue・コメント不足・style 残差・不審な top-level 実行は見つからず、残った top-level 文は公開スクリプトとして許容される初期化処理に限られる。scientific official state、paper roadmap completion、reopen conditions は unchanged のままであり、今回の作業は reproducibility source の静的健全性確認と publish-blocker 切り分けに限られる。
[work] 2026-04-08T11:16:29+00:00 ユーザー指示『進めてください』に対応し、今回の branch は computation ではなく **release-target canonical public artifact curation + public-output commit creation** と判定した。理由は、新しい理論計算や検証更新ではなく、release 前に `output/public` のうち論文・README・公開レジストリに実際に結び付く canonical artifact だけを切り出して commit し、`tmp_*` や内部監査補助物を公開履歴から分離する pure release-prep task だったからである。まず `git status --short output/public` を確認し、`output/public/gw/tmp_*`、`output/public/quantum/*declaration_gate*`、`output/public/quantum/*route_sync*` などの不要候補が大量に混在していることを確認した。ついで `doc/paper/*.md`、[README.md](C:/develop/waveP/README.md)、[doc/PUBLISHING.md](C:/develop/waveP/doc/PUBLISHING.md)、[doc/paper/README.md](C:/develop/waveP/doc/paper/README.md) から `output/public/...` 参照を抽出し、論文面と公開 README から直接参照される artifact set を起点に canonical public publish set を構築した。curated manifest は [release_public_artifact_publish_set.txt](C:/develop/waveP/output/private/summary/release_public_artifact_publish_set.txt) に保存し、`tracked_keep=924`、`untracked_keep=66`、`total=990` を固定した。抽出条件は「tracked changed file は `tmp_` / `declaration_gate` / `route_sync` を除いて保持」「untracked file は doc で直接参照されたものか、その same-stem sibling に限る」であり、`output/public/summary/part5_conceptual_figures_metrics.json` と [part5_future_predictions_timeline_metrics.json](C:/develop/waveP/output/public/summary/part5_future_predictions_timeline_metrics.json) を明示的 sidecar として加えた。次に manifest を使って `git add` を実行し、staged set の topic breakdown が `cassini=20, cosmology=42, eht=23, gps=8, gw=21, llr=50, mercury=2, pulsar=3, quantum=722, summary=34, theory=33, viking=2, vlbi=26, xrism=4`、`TOTAL=990` であることを確認した。さらに staged set に対し `tmp_ / declaration_gate / route_sync / retry_refresh / debugraw / envprobe / probe / blocked_interface_stack / rerun_policy / public_wait_data_watchpack` の再検索を行い、publish set から除外されていることを確認した。唯一 `operational_cycle` を含む [lagrangian_noether_closure_operational_cycle_8722_8721_watch_policy.json](C:/develop/waveP/output/public/quantum/lagrangian_noether_closure_operational_cycle_8722_8721_watch_policy.json) は [01_figures_index.md](C:/develop/waveP/doc/paper/01_figures_index.md) と [13_part4_verification.md](C:/develop/waveP/doc/paper/13_part4_verification.md) から paper-linked canonical artifact であることを確認して保持した。以上を踏まえ、staged canonical public artifact を `git commit -m "release: add canonical public artifacts for paper v2.0"` で commit し、commit hash [7ec8481](C:/develop/waveP/.git/refs/heads/main) を得た。これにより local `main` は `origin/main` に対して `ahead 3` となった。scientific official state、paper roadmap completion、reopen conditions は unchanged のままであり、今回の作業は release 用 canonical public output の切り分けと commit 作成に限られる。残作業は、dirty tree に残る `tmp_*` / 内部監査補助物 / 未選別 public artifact を push 前に最終整理することである。
[work] 2026-04-08T12:17:21+00:00 ユーザー指示『README の正本を root に揃え、public 側へ再同期する方針で進める』に対応し、今回の branch は computation ではなく **release push completion + SSH-over-443 stabilization + root/public README canonical resync** と判定した。理由は、新しい理論計算や検証更新ではなく、release の公開経路を確定し、push 後に見つかった README の canonical drift を解消する pure release-ops task だったからである。まず push 前の dirty tree は release に不要な残差を含んでいたため、`pre-tag leftover untracked scripts` と `pre-tag leftover public temp artifacts` の 2 本の `git stash` に退避し、解析再開用の退避資産として保持する方針を固定した。そのうえで `v0.2.0` annotated tag を作成し、GitHub への push は HTTPS 認証失敗を受けて SSH へ切り替えた。企業 proxy/firewall により port 22 は閉じていたため、repo local の `core.sshCommand` を `ssh.github.com:443`、`C:/Users/ogawa/.ssh/id_ed25519_wavep_release`、repo-local `known_hosts` を使う構成へ切り替え、ユーザーが GitHub に公開鍵を登録した後に `ssh -T ... -o Hostname=ssh.github.com -p 443 git@github.com` で疎通確認を行った。続けて `git push origin main` と `git push origin v0.2.0` を実行し、remote `main` が commit [a97628c](C:/develop/waveP/.git/refs/heads/main)、remote tag `v0.2.0` が tag object `ac09bfa` を指していることを確認した。push 後、root [README.md](C:/develop/waveP/README.md) と [output/public/README.md](C:/develop/waveP/output/public/README.md) の内容差を確認したところ、public 側だけに `W/Z ボソン質量を理論内部から導出` の特徴段落と、末尾の `数式閉包` をもう少し展開した締め文が残っていた。root README を正本とする repo ルールに従い、これら 2 箇所を root [README.md](C:/develop/waveP/README.md) に取り込み、`python -B scripts/summary/sync_public_readme.py --direction root-to-public` を実行して public 側へ再同期した。同期後は inline Python 比較で root/public README が byte 内容一致であることを確認した。scientific official state、paper roadmap completion、reopen conditions は unchanged のままであり、今回の作業は release の公開経路を SSH over 443 へ確定し、push 完了後の README canonical drift を解消する release-surface final sync に限られる。
